language: zh
workspace: ./workspace
log_dir: ./temp/model_responses
max_turns: 300

tools:
  # 留空表示使用历史默认行为：注册全部工具组，避免 SOP 命中后模型看不到对应工具。
  # 如果后续需要轻量模式，可显式设置为 [core, lsp, browser, memory, skill, ask]。
  enabled_groups: []

llm:
  active_profile: deepseek
  # fallback_profiles: [local]
  profiles:
    deepseek:
      provider: openai
      name: deepseek
      api_key: ${DEEPSEEK_API_KEY}
      api_base: https://api.deepseek.com
      model: deepseek-v4-pro
      stream: true
      connect_timeout_seconds: 10
      read_timeout_seconds: 120
      max_retries: 2

    local:
      provider: openai
      name: local
      api_key: ${LOCAL_OPENAI_API_KEY}
      api_base: http://127.0.0.1:11434/v1
      model: qwen3-coder
      stream: true
      connect_timeout_seconds: 10
      read_timeout_seconds: 120
      max_retries: 1

context:
  max_history_messages: 40
  keep_recent_tool_results: 2
  max_tool_result_chars: 12000
  compacted_tool_head_chars: 4000
  compacted_tool_tail_chars: 4000
  max_request_chars: 100000
  max_session_memory_chars: 20000
  max_compact_summary_chars: 60000
  enable_micro_compact: true

observability:
  langfuse:
    enabled: ${COHORT_LANGFUSE_ENABLED}
    host: ${LANGFUSE_HOST}
    public_key: ${LANGFUSE_PUBLIC_KEY}
    secret_key: ${LANGFUSE_SECRET_KEY}
    environment: ${COHORT_ENV}
    release: ${COHORT_RELEASE}
    timeout_seconds: 2
